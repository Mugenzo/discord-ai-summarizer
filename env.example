# Discord Bot Token (get from https://discord.com/developers/applications)
DISCORD_BOT_TOKEN=your_bot_token_here

# Ollama API URL (default: http://localhost:11434)
OLLAMA_API_URL=http://localhost:11434

# Model name for summarization (e.g., llama2, mistral, llama3)
OLLAMA_MODEL=llama3

# Whisper model for transcription (tiny, base, small, medium, large, large-v2, large-v3)
# Recommended: medium (good balance) or large-v3 (best accuracy, slower)
# For NVIDIA RTX GPUs with 16GB+ VRAM: large-v3 (best accuracy, fast with GPU)
# For NVIDIA RTX GPUs with 8GB VRAM: medium (good balance)
# For CPU-only or M1 Mac with 32GB RAM: large-v3 (best accuracy, slower on CPU)
WHISPER_MODEL=large-v3

# Optional: Enable debug mode (verbose logging)
# Set to true, 1, or yes to enable debug mode
# DEBUG=false

