py-cord[voice]>=2.5.0
aiohttp>=3.9.0
python-dotenv>=1.0.0
ollama>=0.1.0
PyNaCl>=1.5.0
pydub>=0.25.1
openai-whisper>=20231117
langdetect>=1.0.9
# PyTorch with CUDA support (for GPU acceleration)
# Note: Install PyTorch with CUDA from https://pytorch.org/get-started/locally/
# For CUDA 12.x: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
# For CUDA 11.x: pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# Whisper will automatically use GPU if PyTorch with CUDA is installed
